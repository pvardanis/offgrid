"""What offgrid knows, and what it asks of the things it talks to.

What a machine is, how much room it has, which models fit, what a runtime and
an agent are each asked for, and what is remembered between runs. It knows
nothing about any adapter: `import-linter` states that over this package, so
every module beneath it is covered by being beneath it.
"""
