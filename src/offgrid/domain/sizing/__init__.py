"""What this machine has room for, and which published models fit in it.

From the Mac itself through to the sentences `offgrid recommend` prints: how
much room there is, how fast weights are read, what a published list says a
model is, and which of them are worth running here.

It knows nothing about running one. Not a module in here imports anything from
`running/`, and a contract says so.
"""
