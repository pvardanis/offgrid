"""What a run is made of: a runtime, an agent, and the model between them.

What offgrid asks of each of the two, the vocabulary they trade in — a model, a
dialect, what a runtime can be asked to do, what an agent can reach — and
making the model that answers be the one that was wanted.

It knows nothing about what fits. Not a module in here imports anything from
`sizing/`, and a contract says so.
"""
