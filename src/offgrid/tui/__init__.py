"""The screen offgrid opens when it is run with nothing to do.

A delivery layer beside `cli/`: both show what the domain answered, and the
screen does not know the command line exists, which is the direction the
contract states. What is shown here is read by whoever opens the screen and
handed in, so the picker names no adapter and no registry.
"""
