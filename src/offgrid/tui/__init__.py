"""The screen offgrid opens when it is run with nothing to do.

A delivery layer beside `cli/`: both show what the domain answered, and
neither knows the other exists. What is shown here is read by whoever opens
the screen and handed in, so the picker names no adapter and no registry.
"""
